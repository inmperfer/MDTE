?meuse
if (!require('sp')) install.packages('sp'); library('sp')
if (!require('lattice')) install.packages('lattice'); library('lattice')
if (!require('xts')) install.packages('xts'); library('xts')
if (!require('gstat')) install.packages('gstat'); library('gstat')
?meuse
data(meuse)
names(meuse)
dim(meuse)
summary(log(meuse$lead))
summary(log(meuse$zinc))
summary(log(meuse$lead))
hist(meuse$lead, breaks = seq(0, 1900, by = 100), col = "lightblue",border = "red", main = "Concentración de plomo (peso: ppm)")
hist(log(meuse$lead), breaks = seq(3, 7, by = 0.3), col = "lightblue",border = "red", main = "Log-Concentración de plomo (peso: ppm)")
rug(log(meuse$lead))
